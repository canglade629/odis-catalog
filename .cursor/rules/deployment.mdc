---
alwaysApply: true
---
Deploy the frontend and backend API together as a single service on Coolify.

Do not use Cloud Run, Cloud Build, or Terraform for deployments in this repository.

Build Docker images locally, then deploy through the existing Coolify workflow.

The app runtime uses Scaleway S3 + PostgreSQL (Coolify-managed infrastructure).