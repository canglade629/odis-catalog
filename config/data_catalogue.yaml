# Catalogue des données - Couche Silver
# Descriptions des champs pour toutes les tables de la couche Silver
# Généré à partir des données réelles le 2025-12-03

version: "1.0"
generated_at: "2025-12-03T17:36:00Z"

tables:
  
  # ============================================================================
  # TABLES DE DIMENSION
  # ============================================================================
  
  dim_commune:
    description: "Table de dimension des communes françaises avec codes INSEE, départements et régions. Master Data Management (MDM) pour toutes les jointures géographiques."
    row_count: 34935
    fields:
      commune_sk:
        type: string
        nullable: false
        description: "Clé surrogate unique (hash MD5 du code INSEE). Clé primaire utilisée pour toutes les jointures géographiques."
        example: "68368e664a4422a17d55658577368fbe"
        
      commune_insee_code:
        type: string
        nullable: false
        description: "Code INSEE de la commune à 5 chiffres (format officiel). Pour les DOM-TOM, commence par 97x ou 98x."
        example: "01054"
        
      commune_label:
        type: string
        nullable: false
        description: "Nom officiel standardisé de la commune tel qu'enregistré par l'INSEE."
        example: "Bourg-Saint-Christophe"
        
      departement_code:
        type: string
        nullable: false
        description: "Code département à 2 chiffres (3 pour les DOM-TOM). Extrait des 2-3 premiers caractères du code INSEE."
        example: "01"
        
      region_code:
        type: string
        nullable: false
        description: "Code région administrative (nomenclature 2016). Mappé depuis le département selon la réforme territoriale."
        example: "84"
        
      job_metadata:
        type: json
        nullable: false
        description: "Métadonnées de traçabilité (job_insert_id, job_insert_date_utc, job_modify_id, job_modify_date_utc, ingestion_timestamp) au format JSON."
        example: '{"job_insert_id": "silver_geo", "job_insert_date_utc": "2025-12-03T17:26:51.089Z", "job_modify_id": "silver_geo", "job_modify_date_utc": "2025-12-03T17:26:51.089Z", "ingestion_timestamp": "2025-12-03T15:00:00.000Z"}'


  ref_logement_profil:
    description: "Table de référence des profils de biens immobiliers (type, typologie, surfaces) pour les données 2024. Utilisée pour jointure via logement_profil_sk."
    row_count: 4
    fields:
      logement_profil_sk:
        type: string
        nullable: false
        description: "Clé surrogate unique (hash MD5 de source_filename). Clé primaire utilisée pour jointure avec fact_loyer_annonce."
        example: "9810bbf965b83ba99c6f45aeb8d440a0"
        
      source_filename:
        type: string
        nullable: false
        description: "Nom du fichier source des données (ex: 'pred-app-mef-dhup.csv')."
        example: "pred-app-mef-dhup.csv"
        
      annee:
        type: integer
        nullable: false
        description: "Année de référence des données (2024 pour les données actuelles)."
        example: 2024
        
      logement_type:
        type: string
        nullable: false
        description: "Type de bien immobilier: 'appartement' ou 'maison'."
        example: "appartement"
        
      typologie:
        type: string
        nullable: false
        description: "Segment typologique du bien: 'toutes typologies', 'T1 et T2', ou 'T3 et plus'."
        example: "toutes typologies"
        
      surface:
        type: decimal(10,2)
        nullable: false
        description: "Surface de référence en m² utilisée pour le calcul du loyer moyen pour ce segment."
        example: 52.0
        
      surface_piece_moy:
        type: decimal(10,2)
        nullable: false
        description: "Surface moyenne par pièce en m² pour ce segment typologique."
        example: 22.2
        
      job_metadata:
        type: json
        nullable: false
        description: "Métadonnées de traçabilité (job_insert_id, job_insert_date_utc, job_modify_id, job_modify_date_utc) au format JSON."
        example: '{"job_insert_id": "ref_logement_profil", "job_insert_date_utc": "2025-12-05T10:00:00.000Z", "job_modify_id": "ref_logement_profil", "job_modify_date_utc": "2025-12-05T10:00:00.000Z"}'


  dim_accueillant:
    description: "Table de dimension des structures d'accueil avec enrichissement géographique (FK vers dim_commune) et coordonnées GPS."
    row_count: 1293
    fields:
      accueillant_sk:
        type: string
        nullable: false
        description: "Clé surrogate unique (hash MD5 de latitude + longitude + statut). Clé primaire."
        example: "34396592d9b4e4923bc8e860372e2c26"
        
      commune_sk:
        type: string
        nullable: true
        description: "Clé étrangère vers dim_commune. Permet de faire le lien avec la commune via le code postal (5 premiers caractères)."
        example: "b344953b75615eb41588c6753a3899e9"
        
      statut:
        type: string
        nullable: false
        description: "Statut actuel de la structure d'accueil dans le processus (ex: 'Bilan à faire', 'En attente de mise en relation')."
        example: "Bilan à faire"
        
      latitude:
        type: double
        nullable: false
        description: "Latitude géographique en degrés décimaux (système WGS84). Comprise entre -90 et 90."
        example: 43.28391
        
      longitude:
        type: double
        nullable: false
        description: "Longitude géographique en degrés décimaux (système WGS84). Comprise entre -180 et 180."
        example: 5.356134
        
      job_metadata:
        type: json
        nullable: false
        description: "Métadonnées de traçabilité (job_insert_id, job_insert_date_utc, job_modify_id, job_modify_date_utc, ingestion_timestamp) au format JSON."
        example: '{"job_insert_id": "dim_accueillant", "job_insert_date_utc": "2025-12-03T17:26:54.263Z", "job_modify_id": "dim_accueillant", "job_modify_date_utc": "2025-12-03T17:26:54.263Z", "ingestion_timestamp": "2025-12-03T15:00:00.000Z"}'


  dim_gare_segment:
    description: "Table de référence des segments de gares (A=National, B=Régional, C=Local). Table hardcodée avec 3 lignes."
    row_count: 3
    fields:
      gare_segment_sk:
        type: string
        nullable: false
        description: "Clé surrogate unique (hash MD5 du code segment). Clé primaire utilisée pour jointure avec dim_gare."
        example: "7fc56270e7a70fa81a5935b72eacbe29"
        
      gare_segment_code:
        type: string
        nullable: false
        description: "Code du segment de gare: 'A' (National), 'B' (Régional), ou 'C' (Local)."
        example: "A"
        
      gare_segment_label:
        type: string
        nullable: false
        description: "Libellé du segment de gare."
        example: "Gare d'intérêt national"
        
      gare_segment_description:
        type: string
        nullable: false
        description: "Description détaillée du segment avec critères de classification."
        example: "Gares dont la fréquentation voyageurs nationaux/internationaux est ≥ 250 000 par an ou représente 100% du trafic"
        
      job_metadata:
        type: json
        nullable: false
        description: "Métadonnées de traçabilité (job_insert_id, job_insert_date_utc, job_modify_id, job_modify_date_utc) au format JSON."
        example: '{"job_insert_id": "dim_gare_segment", "job_insert_date_utc": "2025-12-05T13:50:00.000Z", "job_modify_id": "dim_gare_segment", "job_modify_date_utc": "2025-12-05T13:50:00.000Z"}'


  dim_gare:
    description: "Table de dimension des gares voyageurs avec enrichissement géographique (FK vers dim_commune et dim_gare_segment) et coordonnées GPS."
    row_count: 2785
    fields:
      gare_sk:
        type: string
        nullable: false
        description: "Clé surrogate unique (hash MD5 du trigramme). Clé primaire."
        example: "03753033274ae40987ca3689f75a13dd"
        
      commune_sk:
        type: string
        nullable: true
        description: "Clé étrangère vers dim_commune. Enrichissement géographique via le code commune de la gare."
        example: "b344953b75615eb41588c6753a3899e9"
        
      gare_segment_sk:
        type: string
        nullable: false
        description: "Clé étrangère vers dim_gare_segment. Classification de la gare (National/Régional/Local) après nettoyage avec logique de priorité A>B>C."
        example: "7fc56270e7a70fa81a5935b72eacbe29"
        
      gare_code:
        type: string
        nullable: false
        description: "Code trigramme de la gare (identifiant unique court, ex: 'PAR' pour Paris)."
        example: "PAR"
        
      gare_label:
        type: string
        nullable: false
        description: "Nom de la gare tel qu'utilisé par SNCF."
        example: "Paris-Gare-de-Lyon"
        
      latitude:
        type: double
        nullable: false
        description: "Latitude géographique en degrés décimaux (système WGS84). Comprise entre -90 et 90."
        example: 48.8444
        
      longitude:
        type: double
        nullable: false
        description: "Longitude géographique en degrés décimaux (système WGS84). Comprise entre -180 et 180."
        example: 2.3732
        
      job_metadata:
        type: json
        nullable: false
        description: "Métadonnées de traçabilité (job_insert_id, job_insert_date_utc, job_modify_id, job_modify_date_utc, ingestion_timestamp) au format JSON."
        example: '{"job_insert_id": "dim_gare", "job_insert_date_utc": "2025-12-05T13:46:26.000Z", "job_modify_id": "dim_gare", "job_modify_date_utc": "2025-12-05T13:46:26.000Z", "ingestion_timestamp": "2025-12-05T12:00:00.000Z"}'


  dim_ligne:
    description: "Table de dimension des lignes ferroviaires avec catégories (TGV/non-TGV) et coordonnées géographiques."
    row_count: 933
    fields:
      ligne_sk:
        type: string
        nullable: false
        description: "Clé surrogate unique (hash MD5 du code ligne). Clé primaire."
        
      ligne_code:
        type: string
        nullable: false
        description: "Code officiel de la ligne ferroviaire (6 chiffres)."
        
      ligne_label:
        type: string
        nullable: false
        description: "Libellé/nom officiel de la ligne ferroviaire."
        
      categorie:
        type: string
        nullable: false
        description: "Catégorie de la ligne (ex: 'Ligne à grande vitesse', 'Ligne classique')."
        
      is_tgv:
        type: boolean
        nullable: false
        description: "Indicateur true si la ligne est une LGV (Ligne à Grande Vitesse), false sinon."
        
      geo_shape_coordinates:
        type: string
        nullable: true
        description: "Coordonnées GeoJSON de la ligne."
        
      job_metadata:
        type: json
        nullable: false
        description: "Métadonnées de traçabilité (job_insert_id, job_insert_date_utc, job_modify_id, job_modify_date_utc, ingestion_timestamp) au format JSON."
        example: '{"job_insert_id": "dim_ligne", "job_insert_date_utc": "2025-12-03T17:26:57.000Z", "job_modify_id": "dim_ligne", "job_modify_date_utc": "2025-12-03T17:26:57.000Z", "ingestion_timestamp": "2025-12-03T15:00:00.000Z"}'


  dim_siae_structure:
    description: "Table de dimension des structures d'insertion par l'activité économique (SIAE) avec enrichissement géographique (FK vers dim_commune) et informations de contact."
    row_count: 86374
    fields:
      siae_structure_sk:
        type: string
        nullable: false
        description: "Clé surrogate unique (hash MD5 de l'ID). Clé primaire."
        
      siae_structure_bk:
        type: string
        nullable: false
        description: "Business key - identifiant unique original de la structure dans le système source."
        
      siae_structure_siret_code:
        type: string
        nullable: false
        description: "Code SIRET - numéro SIRET de la structure (14 chiffres), identifiant officiel d'établissement."
        
      siae_structure_label:
        type: string
        nullable: false
        description: "Nom/libellé de la structure."
        
      siae_structure_description:
        type: string
        nullable: true
        description: "Description textuelle des activités de la structure."
        
      commune_sk:
        type: string
        nullable: true
        description: "Clé étrangère vers dim_commune. Hash MD5 du nom de commune."
        
      code_postal:
        type: string
        nullable: false
        description: "Code postal de la structure."
        
      adresse:
        type: string
        nullable: false
        description: "Adresse postale principale de la structure."
        
      complement_adresse:
        type: string
        nullable: true
        description: "Complément d'adresse (bâtiment, étage, etc.)."
        
      longitude:
        type: double
        nullable: true
        description: "Coordonnée géographique - longitude."
        
      latitude:
        type: double
        nullable: true
        description: "Coordonnée géographique - latitude."
        
      telephone:
        type: string
        nullable: true
        description: "Numéro de téléphone de contact de la structure."
        
      courriel:
        type: string
        nullable: true
        description: "Adresse email de contact de la structure."
        
      site_web:
        type: string
        nullable: true
        description: "URL du site web de la structure."
        
      job_metadata:
        type: json
        nullable: false
        description: "Métadonnées de traçabilité (job_insert_id, job_insert_date_utc, job_modify_id, job_modify_date_utc) au format JSON."
        example: '{"job_insert_id": "silver_siae_structures", "job_insert_date_utc": "2025-12-03T17:27:00.000Z", "job_modify_id": "silver_siae_structures", "job_modify_date_utc": "2025-12-03T17:27:00.000Z"}'


  # ============================================================================
  # TABLES DE FAITS
  # ============================================================================
  
  fact_loyer_annonce:
    description: "Table de faits des loyers d'annonce au m² par commune avec bornes de prédiction, niveau d'observation et indicateurs de qualité. FK vers dim_commune et ref_logement_profil."
    row_count: 139672
    fields:
      commune_sk:
        type: string
        nullable: false
        description: "Clé étrangère vers dim_commune. Permet de joindre avec les données géographiques (département, région, EPCI via dim_commune)."
        example: "b611890bcbe01216a7d0a58a1ec554a6"
        
      logement_profil_sk:
        type: string
        nullable: true
        description: "Clé étrangère vers ref_logement_profil (hash MD5 de source_filename). Permet de récupérer le type de bien, la typologie et les surfaces de référence via jointure."
        example: "9810bbf965b83ba99c6f45aeb8d440a0"
        
      loyer_m2_moy:
        type: decimal(10,2)
        nullable: false
        description: "Loyer mensuel moyen d'annonce au mètre carré en euros (valeur centrale de la prédiction)."
        example: 10.75
        
      loyer_m2_min:
        type: decimal(10,2)
        nullable: false
        description: "Borne inférieure de l'intervalle de prédiction du loyer au m² en euros (confiance 95%)."
        example: 8.44
        
      loyer_m2_max:
        type: decimal(10,2)
        nullable: false
        description: "Borne supérieure de l'intervalle de prédiction du loyer au m² en euros (confiance 95%)."
        example: 13.69
        
      maille_observation:
        type: string
        nullable: false
        description: "Maille géographique d'observation: 'commune' (données réelles de la commune), 'maille' (agrégation géographique), ou autre niveau d'agrégation."
        example: "commune"
        
      score_qualite:
        type: decimal(5,4)
        nullable: true
        description: "R² ajusté du modèle de prédiction. Indicateur de qualité de la prédiction (0 à 1, plus élevé = meilleure qualité)."
        example: 0.8523
        
      nb_observation_maille:
        type: integer
        nullable: true
        description: "Nombre d'observations utilisées dans la maille géographique pour le calcul de la prédiction."
        example: 150
        
      nb_observation_commune:
        type: integer
        nullable: true
        description: "Nombre d'observations réelles au niveau de la commune."
        example: 42
        
      job_metadata:
        type: json
        nullable: false
        description: "Métadonnées de traçabilité (job_insert_id, job_insert_date_utc, job_modify_id, job_modify_date_utc, ingestion_timestamp) au format JSON."
        example: '{"job_insert_id": "fact_loyer_annonce", "job_insert_date_utc": "2025-12-05T17:27:05.278Z", "job_modify_id": "fact_loyer_annonce", "job_modify_date_utc": "2025-12-05T17:27:05.278Z", "ingestion_timestamp": "2025-12-05T15:00:00.000Z"}'




  fact_zone_attraction:
    description: "Table de faits des zones d'attraction des villes 2020 avec FKs vers communes (commune et pôle) et catégories d'attraction."
    row_count: 26209
    fields:
      zone_attraction_sk:
        type: string
        nullable: false
        description: "Clé surrogate unique (hash MD5 de CODGEO + CODEAAV + CODGEOAAV). Clé primaire."
        
      commune_sk:
        type: string
        nullable: false
        description: "Clé étrangère vers dim_commune. Identifie la commune concernée par la zone d'attraction."
        
      commune_pole_sk:
        type: string
        nullable: false
        description: "Clé étrangère vers dim_commune. Identifie la commune pôle (centre de la zone d'attraction)."
        
      zone_attraction_code:
        type: string
        nullable: false
        description: "Code de la zone d'attraction des villes 2020 (nomenclature INSEE)."
        
      zone_attraction_label:
        type: string
        nullable: false
        description: "Nom de la zone d'attraction (généralement le nom de la ville pôle). Les mentions '(partie française)' ont été retirées."
        
      zone_attraction_categorie:
        type: string
        nullable: false
        description: "Catégorie de la zone d'attraction selon sa taille démographique (ex: 'Grande aire', 'Moyenne aire', 'Petite aire')."
        
      job_metadata:
        type: json
        nullable: false
        description: "Métadonnées de traçabilité (job_insert_id, job_insert_date_utc, job_modify_id, job_modify_date_utc, ingestion_timestamp) au format JSON."
        example: '{"job_insert_id": "fact_zone_attraction", "job_insert_date_utc": "2025-12-03T17:27:02.000Z", "job_modify_id": "fact_zone_attraction", "job_modify_date_utc": "2025-12-03T17:27:02.000Z", "ingestion_timestamp": "2025-12-03T15:00:00.000Z"}'


  fact_siae_poste:
    description: "Table de faits des postes ouverts en SIAE avec codes ROME, types de contrats et disponibilité. FK vers dim_siae_structure."
    row_count: 4294
    fields:
      siae_poste_sk:
        type: string
        nullable: false
        description: "Clé surrogate unique (hash MD5 de poste_id + structure_id). Clé primaire."
        example: "aff636611a73bf370abfbdf429bd00a5"
        
      siae_structure_sk:
        type: string
        nullable: false
        description: "Clé étrangère vers dim_siae_structure. Lie le poste à sa structure SIAE."
        example: "47ea585ea64fc56c67a2ab8ef0f144e0"
        
      poste_id:
        type: string
        nullable: false
        description: "Identifiant unique du poste dans le système source."
        example: "9474"
        
      structure_id:
        type: string
        nullable: false
        description: "Identifiant UUID de la structure parente (redondant avec FK pour référence)."
        example: "6d36ae53-f127-4405-a147-6c2979922964"
        
      structure_siret:
        type: string
        nullable: true
        description: "Numéro SIRET de la structure (redondant, pour faciliter les jointures)."
        example: "20004152300016"
        
      rome_code:
        type: string
        nullable: false
        description: "Code ROME du métier (Répertoire Opérationnel des Métiers et des Emplois de Pôle Emploi). Format: 1 lettre + 4 chiffres."
        example: "A1202"
        
      intitule_poste:
        type: string
        nullable: false
        description: "Intitulé/titre du poste. Peut être l'appellation modifiée ou l'intitulé ROME par défaut."
        example: "Agent d'aménagement paysager"
        
      description_poste:
        type: string
        nullable: true
        description: "Description détaillée du poste, des missions et des conditions de travail."
        example: "Plantation de fleurs en massif..."
        
      contrat_type:
        type: string
        nullable: false
        description: "Type de contrat proposé (ex: 'CDD insertion', 'CDI', 'Contrat aidé')."
        example: "CDD insertion"
        
      poste_disponible:
        type: boolean
        nullable: false
        description: "Indique si le poste est actuellement ouvert au recrutement (true) ou non (false)."
        example: true
        
      postes_nombre:
        type: integer
        nullable: false
        description: "Nombre de postes ouverts pour cette offre."
        example: 1
        
      creation_date_utc:
        type: timestamp
        nullable: true
        description: "Date et heure de création de l'offre de poste dans le système source."
        example: "2020-11-13 16:50:51.584467"
        
      modification_date_utc:
        type: timestamp
        nullable: true
        description: "Date et heure de dernière modification de l'offre dans le système source."
        example: "2025-12-02 07:25:17.755128"
        
      job_metadata:
        type: json
        nullable: false
        description: "Métadonnées de traçabilité (job_insert_id, job_insert_date_utc, job_modify_id, job_modify_date_utc, ingestion_timestamp) au format JSON."
        example: '{"job_insert_id": "fact_siae_poste", "job_insert_date_utc": "2025-12-03T17:27:10.044Z", "job_modify_id": "fact_siae_poste", "job_modify_date_utc": "2025-12-03T17:27:10.044Z", "ingestion_timestamp": "2025-12-03T15:00:00.000Z"}'

# Notes d'utilisation:
# - Toutes les clés surrogates (*_sk) sont des hash MD5 et servent de clés primaires/étrangères
# - Le champ job_metadata est présent dans toutes les tables pour la traçabilité (format JSON)
# - Les coordonnées géographiques utilisent le système WGS84 (sauf mention Lambert 93)
# - Les timestamps sont toujours en UTC
# - Les FK nullable indiquent que l'enrichissement géographique peut échouer

